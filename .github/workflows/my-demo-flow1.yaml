name: my demo flow
on: workflow_dispatch
env:
  my_wf_env: i am the wf variable
jobs:
  my_first_job:
    runs-on: ubuntu-slim
    env:
      my_wf_env: i am the job1 variable
    steps:
      - name: my steps of job1
        env:
          my_wf_env: i am the job1/step1 variable
        run: |
          echo "welcome to job1"
          echo "welcome to my_wf_env is ${{env.my_wf_env}}"
