name: my demo flow
on: workflow_dispatch

jobs:
  my_first_job:
    environment:
      name: Stage      
    runs-on: ubuntu-slim    
    steps:
      - name: my steps of job1        
        run: |
          echo "------org level------"
          echo "${{vars.ORG_VAR}}"
          echo "${{secrets.SECRETE_ORG_VAR}}"
          echo "------------"
          echo "------repo level------"
          echo "${{vars.REPO_VAR}}"
          echo "${{secrets.SECRETE_REPO_VAR}}"
          echo "------------"
          echo "------environment i,e stage level------"
          echo "${{vars.STAGE_VAR}}"
          echo "${{secrets.STAGE_SECRETE_VAR}}"
          echo "------------"
